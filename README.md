use this script to log all the running programs in background quietly from a usb drive, the program stores a looping_programs.txt file that stores the running programs information. 

Autorun.inf file was made so that this can be flashed to a CD drive and ran from that automatically 
